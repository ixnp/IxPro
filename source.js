var webDevProjects []

function DevProject(proObj){
  this.name = proObj.name;
  this.about = proObj.about;
  this.image = proObj.image;
  this.git = probObj.git;
}

devProject.prototype.toHtml = function(){
  var $DevProject = $('ul.projectsContainer').clone();
});

$newDevProject.find('li.name')attr(this.name);
$newDevProject.find('li.about')attr(this.about);
$newDevProject.find('a').attr('href',this.name);
$newDevProject.find('img')attr(this.imge);



//display reminder to self review end of lab02//

/////
